apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: edusubmit-production
  labels:
    app: edusubmit-backend
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: edusubmit-backend
