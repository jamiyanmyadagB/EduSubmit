apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: edusubmit-production
  labels:
    app: edusubmit-ai
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    app: edusubmit-ai
